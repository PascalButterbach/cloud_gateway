server:
  port: 9000


spring:
  application:
    name: API-GATEWAY
  cloud:
    gateway:
      routes:
        - id: USER-SERVICE
          uri: lb://USER-SERVICE
          predicates:
          - Path=/user/**
        - id: IMAGE-SERVICE
          uri: lb://IMAGE-SERVICE
          predicates:
            - Path=/image/**
          filters:
            - AuthFilter

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defeaultZone: http://172.26.208.1:8761/eureka
